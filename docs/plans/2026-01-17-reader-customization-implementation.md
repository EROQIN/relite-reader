# Plan: Reader Customization + Clean UI (Implementation)

## Tasks
1. Extend reader prefs with background + brightness, update defaults and tests.
2. Add backend sync helper with token-based fetch (optional).
3. Update reader controls to include background/brightness UI and theme extras.
4. Apply background/brightness styles to reader surface.
5. Run frontend tests and verify reader page render.
