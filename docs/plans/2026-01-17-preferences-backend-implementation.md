# Plan: Backend Preferences Storage + API (Implementation)

## Tasks
1. Create preferences package (models, defaults, store interface, memory store, file store).
2. Add HTTP handler for GET/PUT preferences with auth.
3. Wire preferences store + handler into router and server.
4. Add tests for store + handler + router wiring.
