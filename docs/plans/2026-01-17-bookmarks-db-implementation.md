# Plan: Bookmarks + DB Progress (Implementation)

## Tasks
1. Add Postgres progress store and wire when `RELITE_DATABASE_URL` is set.
2. Add bookmarks package with memory + Postgres stores.
3. Add bookmarks HTTP handler + router wiring + tests.
4. Add frontend bookmark API + local store + TXT reader UI.
5. Update README and tests.
6. Run backend + frontend tests.
