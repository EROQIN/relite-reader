# Plan: Reader Custom Presets (Implementation)

## Tasks
1. Extend `readerPrefs` to load/save custom presets with local storage utilities; add helper for creating/updating presets and tests.
2. Update `ReaderPage` to load custom presets, merge with built-ins, and expose handlers for save/rename/delete.
3. Update `ReaderControls` UI to manage custom presets (name input, save button, list with apply/rename/delete).
4. Add CSS for the custom presets section and list rows.
5. Run frontend tests and verify reader page render.
