# Plan: README Delivery Docs

## Goal
Replace the placeholder README with product documentation suitable for launch.

## Sections
- Product overview + features.
- Usage manual (reader, customization, WebDAV setup).
- Deployment manual (env vars, build steps, run commands).
- API summary (auth, WebDAV, books, preferences).
- Notes on persistence (memory vs data dir).
